[user]
    name = lvzhandong
    email = lvzhandong@umeng.com
[core]
    excludesfile = /home/larry/.gitignore
    pager = less -+FRSX -FRX
    editor = vim
    autocrlf = input
    safecrlf = true
[alias]
    st = status
    ci = commit
    br = branch
    co = checkout
    sb = show-branch
    lg = log --stat
    last = log --stat -1
    diffstat = diff --stat -r
    wdiff = diff --word-diff
    changes = diff --name-status -r
    unstage = reset HEAD --
    glog = log --graph
    hist = log --pretty=format:\"%C(yellow)%h%Creset %ad %s %C(red)[%an]%Creset\" --graph --date=short
    type = cat-file -t
    dump = cat-file -p
[merge]
    conflictstyle = diff3
    tool = vimdiff
[rerere]
    enabled = true
[push]
    default = upstream
[color]
    ui = true
[i18n]
	commitEncoding = utf8
	logOutputEncoding = utf8
